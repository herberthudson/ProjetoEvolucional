@using ProjetoEvolucional.Models
@model LoginVM
@{
    ViewBag.Title = "Login";
}

<h2>@ViewBag.Title</h2>

<form method="post">
    <div class="form-group">
        <label for="Login">Login:</label>
        <input type="text" asp-for="Login" name="Login" class="form-control" autofocus />
        <small class="is-invalid">
            <span asp-validation-for="LoginVM.Login"></span>
        </small>
    </div>

    <div class="form-group">
        <label for="Senha">Senha:</label>
        <input type="password" asp-for="Senha" name="Senha" class="form-control" />
        <small class="is-invalid">
            <span asp-validation-for="LoginVM.Senha"></span>
        </small>
    </div>

    <button class="btn btn-primary">Entrar</button>

</form>
